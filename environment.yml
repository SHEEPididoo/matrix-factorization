name: recsys-week3
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - numpy>=1.26
  - pandas>=2.1
  - scikit-learn>=1.3
  - scipy>=1.10
  - numba>=0.59
  - pyarrow>=14
  - pip:
      # demo / embedding / llm (optional but recommended for project_template)
      - sentence-transformers>=3.0.0
      - fastapi>=0.110.0
      - uvicorn>=0.29.0
      - streamlit>=1.36.0
      - python-dotenv>=1.0.1
      - openai>=1.50.0
